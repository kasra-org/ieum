# SvelteKit build artifacts
.svelte-kit/
build/

# Node.js dependencies
node_modules/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Lock files for the package managers this project does not use. pnpm-lock.yaml
# is committed on purpose: the frontend image installs from it, so ignoring it
# would leave every build to re-resolve the tree against whatever the registry
# serves that minute.
package-lock.json
yarn.lock

# SvelteKit output
public/build/

# Django specific
*.log
*.pot
*.pyc
__pycache__/
db.sqlite3
media/
staticfiles/
backend/static/
.env

# Python
*.py[cod]
*.so
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
__pypackages__/

# Celery Beat
celerybeat-schedule*